@model OneDriveIntegrator.Services.MicrosoftGraph.Models.Items
@{
    ViewData["Title"] = "Items";
}

<table class="table table-striped table-hover table-sm">
    <thead>
    <tr>
        <th>Name</th>
        <th>Size</th>
        <th>File / Folder</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.Value)
    {
        <tr>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">
                    @item.Name
                </a>
            </td>
            <td>
                @item.Size
            </td>
            <td>
                @if (item.IsFile())
                {
                    <div>File (@item.File!.MimeType)</div>
                }
                else
                {
                    <div>Folder (@item.Folder!.ChildCount)</div>
                }
            </td>
        </tr>
    }
    </tbody>
</table>